import findSolution from "./index";

describe("findSolution", () => {
  test("A", () => {
    expect(findSolution()).toEqual();
  });

  test("B", () => {
    expect(findSolution()).toEqual();
  });

  test("C", () => {
    expect(findSolution()).toEqual();
  });
});
