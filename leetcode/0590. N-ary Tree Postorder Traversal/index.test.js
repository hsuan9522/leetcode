import postorder from "./index";

describe("postorder", () => {
  test("A", () => {
    expect(postorder()).toEqual();
  });

  test("B", () => {
    expect(postorder()).toEqual();
  });

  test("C", () => {
    expect(postorder()).toEqual();
  });
});
