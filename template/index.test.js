import functionName from "./index";

describe("functionName", () => {
  test("A", () => {
    expect(functionName()).toEqual();
  });

  test("B", () => {
    expect(functionName()).toEqual();
  });

  test("C", () => {
    expect(functionName()).toEqual();
  });
});
